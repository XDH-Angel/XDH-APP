<!-- UI/Windows/Panel07/PointToSurfaceWindow.xaml -->
<Window x:Class="LandscapeRevitAddIn.UI.Windows.Panel07.PointToSurfaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="XD Landscape - Align Floor Points to Reference Surface" Height="500" Width="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="White"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <!-- Same style resources as other windows -->
        <LinearGradientBrush x:Key="LogoGradientBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#B8D4C0" Offset="0"/>
            <GradientStop Color="#A8C9B3" Offset="0.3"/>
            <GradientStop Color="#8FBB9A" Offset="0.6"/>
            <GradientStop Color="#7AAE85" Offset="0.8"/>
            <GradientStop Color="#6BA177" Offset="1"/>
        </LinearGradientBrush>

        <SolidColorBrush x:Key="CardBackground" Color="#FEFEFE" Opacity="0.95"/>

        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4A7C5A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3D6349"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5A8C6A"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3A5C45"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#2D5D3A"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A7C5A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="#C8DDD0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.15" Color="#4A7C5A"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource LogoGradientBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="20,20,20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="72" Height="72" VerticalAlignment="Center" Margin="0,0,20,0">
                    <Border.Background>
                        <ImageBrush x:Name="LogoBrush" Stretch="Uniform"/>
                    </Border.Background>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="XD Landscape" FontSize="24" FontWeight="Bold" Foreground="#2D5D3A" Margin="0,0,0,5"/>
                    <TextBlock Text="Align Floor Points to Reference Surface" FontSize="14" Foreground="#4A7C5A"/>
                    <TextBlock Text="Select floor points and align them to a reference surface" FontSize="11" Foreground="#5A8C6A" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main Content Area -->
        <StackPanel Grid.Row="1" Margin="20,10">
            <!-- Selection Area -->
            <Border Style="{StaticResource Card}" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="Selection" FontSize="16" FontWeight="SemiBold" Foreground="#2D5D3A" Margin="0,0,0,15"/>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Floor Points -->
                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                            <TextBlock Text="1. Floor Points to Align:" FontSize="12" Foreground="#4A7C5A" Margin="0,0,0,10"/>
                            <Button Name="SelectPointsButton" Content="📍 Select Floor Points" Style="{StaticResource ModernButton}" Height="40" Click="SelectPointsButton_Click"/>
                            <TextBlock Name="SelectedPointsTextBlock" Margin="0,8,0,0" Text="Click to select floor points" Foreground="#5A8C6A" FontStyle="Italic" TextWrapping="Wrap"/>
                        </StackPanel>

                        <!-- Reference Surface -->
                        <StackPanel Grid.Column="1" Margin="10,0,0,0">
                            <TextBlock Text="2. Reference Surface:" FontSize="12" Foreground="#4A7C5A" Margin="0,0,0,10"/>
                            <Button Name="SelectSurfaceButton" Content="🎯 Select Reference Surface" Style="{StaticResource ModernButton}" Height="40" Click="SelectSurfaceButton_Click" IsEnabled="False"/>
                            <TextBlock Name="SelectedSurfaceTextBlock" Margin="0,8,0,0" Text="Select points first" Foreground="#5A8C6A" FontStyle="Italic" TextWrapping="Wrap"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>

        <!-- Footer with Action Buttons -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="20,0,20,20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Name="CancelButton" Content="Cancel" Style="{StaticResource ModernButton}" Width="100" Height="40" Margin="0,0,10,0" Click="CancelButton_Click"/>
                <Button Name="AlignButton" Content="✅ Align Points" Style="{StaticResource PrimaryButton}" Width="140" Height="40" Click="AlignButton_Click" IsEnabled="False"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>