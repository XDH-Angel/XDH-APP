<Window x:Class="LandscapeRevitAddIn.UI.Windows.Panel08.FilledRegionByFaceWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="XD Landscape - Filled Region by Face" Height="600" Width="700"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="White"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <!-- Logo-Inspired Gradient Background -->
        <LinearGradientBrush x:Key="LogoGradientBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#B8D4C0" Offset="0"/>
            <GradientStop Color="#A8C9B3" Offset="0.3"/>
            <GradientStop Color="#8FBB9A" Offset="0.6"/>
            <GradientStop Color="#7AAE85" Offset="0.8"/>
            <GradientStop Color="#6BA177" Offset="1"/>
        </LinearGradientBrush>

        <!-- Subtle Card Background with slight transparency to show gradient -->
        <SolidColorBrush x:Key="CardBackground" Color="#FEFEFE" Opacity="0.95"/>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4A7C5A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3D6349"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5A8C6A"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3A5C45"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#666666"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Primary Action Button -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#2D5D3A"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A7C5A"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Updated Card Style with subtle transparency -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="#C8DDD0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.15" Color="#4A7C5A"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern ListBox Style -->
        <Style x:Key="ModernListBox" TargetType="ListBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#C8DDD0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ScrollViewer Padding="{TemplateBinding Padding}">
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="8,6"/>
                        <Setter Property="Margin" Value="2"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <Border Background="{TemplateBinding Background}"
                                            BorderBrush="{TemplateBinding BorderBrush}"
                                            BorderThickness="{TemplateBinding BorderThickness}"
                                            Padding="{TemplateBinding Padding}"
                                            CornerRadius="4">
                                        <ContentPresenter/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E8F4EB"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#D4E8D8"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="BorderBrush" Value="#4A7C5A"/>
                                <Setter Property="BorderThickness" Value="1"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Step Number Style -->
        <Style x:Key="StepNumber" TargetType="Ellipse">
            <Setter Property="Fill" Value="#4A7C5A"/>
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Margin" Value="0,0,8,0"/>
        </Style>

        <!-- Step Text Style -->
        <Style x:Key="StepText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Info Text Style -->
        <Style x:Key="InfoText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#5A8C6A"/>
            <Setter Property="FontStyle" Value="Italic"/>
        </Style>

        <!-- Success Text Style -->
        <Style x:Key="SuccessText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#2D5D3A"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource LogoGradientBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header with Logo -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="20,20,20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Logo -->
                <Border Grid.Column="0" Width="72" Height="72"
                        VerticalAlignment="Center" Margin="0,0,20,0">
                    <Border.Background>
                        <ImageBrush x:Name="LogoBrush" Stretch="Uniform"/>
                    </Border.Background>
                </Border>

                <!-- Title and Info -->
                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="XD Landscape"
                               FontSize="24" FontWeight="Bold" Foreground="#2D5D3A"
                               Margin="0,0,0,5"/>
                    <TextBlock Text="Create Filled Regions from Selected Faces"
                               FontSize="14" Foreground="#4A7C5A"/>
                    <TextBlock Text="Convert surface geometry into filled regions for enhanced drafting visualization"
                               FontSize="11" Foreground="#5A8C6A" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Face Selection Instructions -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="20,10,20,10">
            <StackPanel>
                <TextBlock Text="Face Selection Workflow"
                           FontSize="16" FontWeight="SemiBold" Foreground="#2D5D3A"
                           Margin="0,0,0,15"/>

                <!-- Step 1 -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="0,0,12,0">
                        <Ellipse Style="{StaticResource StepNumber}"/>
                        <TextBlock Text="1" Style="{StaticResource StepText}"/>
                    </Grid>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Select Faces" FontWeight="SemiBold" FontSize="12" Foreground="#2D5D3A"/>
                        <TextBlock Text="Click 'Select Faces' to choose the surfaces you want to convert to filled regions" 
                                   FontSize="11" Foreground="#5A8C6A" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>

                <!-- Step 2 -->
                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="0,0,12,0">
                        <Ellipse Style="{StaticResource StepNumber}"/>
                        <TextBlock Text="2" Style="{StaticResource StepText}"/>
                    </Grid>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Choose Filled Region Type" FontWeight="SemiBold" FontSize="12" Foreground="#2D5D3A"/>
                        <TextBlock Text="Select your desired filled region type from the list below" 
                                   FontSize="11" Foreground="#5A8C6A" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>

                <!-- Step 3 -->
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Grid Grid.Column="0" Margin="0,0,12,0">
                        <Ellipse Style="{StaticResource StepNumber}"/>
                        <TextBlock Text="3" Style="{StaticResource StepText}"/>
                    </Grid>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="Create Filled Regions" FontWeight="SemiBold" FontSize="12" Foreground="#2D5D3A"/>
                        <TextBlock Text="Click 'Create Filled Regions' to generate the regions in your current view" 
                                   FontSize="11" Foreground="#5A8C6A" TextWrapping="Wrap"/>
                    </StackPanel>
                </Grid>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Button Name="SelectFacesButton"
                            Grid.Column="0"
                            Content="?? Select Faces"
                            Style="{StaticResource ModernButton}"
                            Height="40" Width="140"
                            Click="SelectFacesButton_Click"/>

                    <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                        <TextBlock Name="SelectedFacesText"
                                   Text="No faces selected"
                                   FontSize="12" Style="{StaticResource InfoText}"/>
                        <TextBlock Text="Use Tab key to cycle through selectable faces"
                                   FontSize="10" Foreground="#7AAE85" Margin="0,2,0,0"/>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <!-- Filled Region Type Selection -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="20,10,20,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Filled Region Type Selection"
                           FontSize="16" FontWeight="SemiBold" Foreground="#2D5D3A"
                           Margin="0,0,0,8"/>

                <TextBlock Grid.Row="1" Text="Choose the pattern and appearance for your filled regions"
                           FontSize="11" Style="{StaticResource InfoText}"
                           Margin="0,0,0,12"/>

                <ListBox Grid.Row="2" Name="FilledRegionTypeListBox"
                         Style="{StaticResource ModernListBox}"
                         SelectionMode="Single"
                         MinHeight="200">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="4,2">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <!-- Pattern Preview -->
                                <Border Grid.Column="0" Width="32" Height="24" 
                                        BorderBrush="#A8C9B3" BorderThickness="1"
                                        CornerRadius="2" Margin="0,0,12,0">
                                    <Rectangle Fill="{Binding PreviewBrush}" 
                                               Stroke="Transparent"/>
                                </Border>

                                <!-- Type Name -->
                                <TextBlock Grid.Column="1" Text="{Binding Name}" 
                                          VerticalAlignment="Center"
                                          FontSize="12" Foreground="#2D5D3A"/>

                                <!-- Type Category -->
                                <TextBlock Grid.Column="2" Text="{Binding Category}" 
                                          VerticalAlignment="Center"
                                          FontSize="10" Style="{StaticResource InfoText}"
                                          Margin="8,0,0,0"/>
                            </Grid>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
        </Border>

        <!-- Footer with Actions --> 
        <Border Grid.Row="3" Style="{StaticResource Card}" Margin="20,10,20,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Summary -->
                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Name="SummaryTextBlock"
                               FontWeight="SemiBold" FontSize="14" Foreground="#2D5D3A"
                               Text="Select faces and a filled region type to continue"/>
                    <TextBlock Text="Filled regions will be created in the current view based on the selected face boundaries"
                               FontSize="11" Foreground="#4A7C5A" Margin="0,2,0,0"/>
                </StackPanel>

                <!-- Action Buttons -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Name="CancelButton" Content="Cancel"
                            Style="{StaticResource ModernButton}"
                            Width="80" Height="38" Margin="0,0,12,0"
                            Click="CancelButton_Click"/>

                    <Button Name="CreateFilledRegionsButton" Content="?? Create Filled Regions"
                            Style="{StaticResource PrimaryButton}"
                            Width="170" Height="38"
                            Click="CreateFilledRegionsButton_Click" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>