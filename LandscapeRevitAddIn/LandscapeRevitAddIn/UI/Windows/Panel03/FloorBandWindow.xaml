<Window x:Class="LandscapeRevitAddIn.UI.Windows.Panel03.FloorBandWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="XD Landscape - Floor Band" Height="700" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="White"
        WindowStyle="SingleBorderWindow"
        MinHeight="500" MinWidth="600">

	<Window.Resources>
		<!-- Logo-Inspired Gradient Background -->
		<LinearGradientBrush x:Key="LogoGradientBackground" StartPoint="0,0" EndPoint="1,1">
			<GradientStop Color="#B8D4C0" Offset="0"/>
			<GradientStop Color="#A8C9B3" Offset="0.3"/>
			<GradientStop Color="#8FBB9A" Offset="0.6"/>
			<GradientStop Color="#7AAE85" Offset="0.8"/>
			<GradientStop Color="#6BA177" Offset="1"/>
		</LinearGradientBrush>

		<!-- Card Background -->
		<SolidColorBrush x:Key="CardBackground" Color="#FEFEFE" Opacity="0.95"/>

		<!-- Modern Button Style -->
		<Style x:Key="ModernButton" TargetType="Button">
			<Setter Property="Background" Value="#4A7C5A"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="BorderBrush" Value="#3D6349"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Padding" Value="12,8"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="FontSize" Value="11"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
							<ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
						</Border>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="#5A8C6A"/>
				</Trigger>
				<Trigger Property="IsPressed" Value="True">
					<Setter Property="Background" Value="#3A5C45"/>
				</Trigger>
				<Trigger Property="IsEnabled" Value="False">
					<Setter Property="Background" Value="#CCCCCC"/>
					<Setter Property="Foreground" Value="#666666"/>
					<Setter Property="Cursor" Value="Arrow"/>
				</Trigger>
			</Style.Triggers>
		</Style>

		<!-- Primary Action Button -->
		<Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
			<Setter Property="Background" Value="#2D5D3A"/>
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="FontSize" Value="12"/>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="#4A7C5A"/>
				</Trigger>
				<Trigger Property="IsEnabled" Value="False">
					<Setter Property="Background" Value="#CCCCCC"/>
					<Setter Property="Foreground" Value="#666666"/>
				</Trigger>
			</Style.Triggers>
		</Style>

		<!-- Card Style -->
		<Style x:Key="Card" TargetType="Border">
			<Setter Property="Background" Value="{StaticResource CardBackground}"/>
			<Setter Property="BorderBrush" Value="#C8DDD0"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="CornerRadius" Value="8"/>
			<Setter Property="Padding" Value="20"/>
			<Setter Property="Effect">
				<Setter.Value>
					<DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.15" Color="#4A7C5A"/>
				</Setter.Value>
			</Setter>
		</Style>

		<!-- Modern ComboBox Style -->
		<Style x:Key="ModernComboBox" TargetType="ComboBox">
			<Setter Property="Background" Value="White"/>
			<Setter Property="BorderBrush" Value="#A8C9B3"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Padding" Value="12,10"/>
			<Setter Property="FontSize" Value="12"/>
			<Setter Property="Height" Value="40"/>
		</Style>

		<!-- Modern TextBox Style -->
		<Style x:Key="ModernTextBox" TargetType="TextBox">
			<Setter Property="Background" Value="White"/>
			<Setter Property="BorderBrush" Value="#A8C9B3"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Padding" Value="12,10"/>
			<Setter Property="FontSize" Value="12"/>
			<Setter Property="Height" Value="40"/>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="BorderBrush" Value="#4A7C5A"/>
				</Trigger>
				<Trigger Property="IsFocused" Value="True">
					<Setter Property="BorderBrush" Value="#2D5D3A"/>
				</Trigger>
			</Style.Triggers>
		</Style>

		<!-- Modern RadioButton Style -->
		<Style x:Key="ModernRadioButton" TargetType="RadioButton">
			<Setter Property="FontSize" Value="12"/>
			<Setter Property="Foreground" Value="#2D5D3A"/>
			<Setter Property="Margin" Value="0,5"/>
			<Setter Property="Padding" Value="8,5"/>
		</Style>

		<!-- Status Text Styles -->
		<Style x:Key="SuccessText" TargetType="TextBlock">
			<Setter Property="Foreground" Value="#2D5D3A"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
		</Style>

		<Style x:Key="InfoText" TargetType="TextBlock">
			<Setter Property="Foreground" Value="#5A8C6A"/>
			<Setter Property="FontStyle" Value="Italic"/>
		</Style>
	</Window.Resources>

	<Grid Background="{StaticResource LogoGradientBackground}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!-- Header -->
		<Border Grid.Row="0" Style="{StaticResource Card}" Margin="20,20,20,10">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>

				<!-- Logo -->
				<Border Grid.Column="0" Width="72" Height="72"
                        VerticalAlignment="Center" Margin="0,0,20,0">
					<Border.Background>
						<ImageBrush x:Name="LogoBrush" Stretch="Uniform"/>
					</Border.Background>
				</Border>

				<!-- Title and Info -->
				<StackPanel Grid.Column="1" VerticalAlignment="Center">
					<TextBlock Text="XD Landscape"
                               FontSize="24" FontWeight="Bold" Foreground="#2D5D3A"
                               Margin="0,0,0,5"/>
					<TextBlock Text="Create Floor Band"
                               FontSize="14" Foreground="#4A7C5A"/>
					<TextBlock Text="Create a new floor band based on existing floor perimeter with offset"
                               FontSize="11" Foreground="#5A8C6A" Margin="0,2,0,0"/>
				</StackPanel>
			</Grid>
		</Border>

		<!-- Floor Selection -->
		<Border Grid.Row="1" Style="{StaticResource Card}" Margin="20,10,20,10">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<!-- Section Title -->
				<TextBlock Grid.Row="0" Text="Floor Selection"
                           FontSize="16" FontWeight="SemiBold" Foreground="#2D5D3A"
                           Margin="0,0,0,15"/>

				<!-- Select Floor Button -->
				<Button Grid.Row="1" Name="SelectFloorButton"
                        Content="📐 Select Existing Floor"
                        Style="{StaticResource ModernButton}"
                        Height="42" Margin="0,0,0,10"
                        Click="SelectFloorButton_Click"/>

				<!-- Selected Floor Info -->
				<TextBlock Grid.Row="2" Name="SelectedFloorTextBlock"
                           Text="Click button above to select a floor"
                           Style="{StaticResource InfoText}" Margin="0,5,0,0"/>
			</Grid>
		</Border>

		<!-- Offset Configuration -->
		<Border Grid.Row="2" Style="{StaticResource Card}" Margin="20,10,20,10">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<!-- Section Title -->
				<TextBlock Grid.Row="0" Text="Offset Configuration"
                           FontSize="16" FontWeight="SemiBold" Foreground="#2D5D3A"
                           Margin="0,0,0,15"/>

				<!-- Offset Distance -->
				<Grid Grid.Row="1" Margin="0,0,0,15">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="Auto"/>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="Auto"/>
					</Grid.ColumnDefinitions>

					<TextBlock Grid.Column="0" Text="Offset Distance:"
                               FontSize="12" Foreground="#2D5D3A"
                               VerticalAlignment="Center" Margin="0,0,15,0"/>

					<TextBox Grid.Column="1" Name="OffsetDistanceTextBox"
                             Style="{StaticResource ModernTextBox}"
                             Text="5.0" TextChanged="OffsetDistanceTextBox_TextChanged"/>

					<TextBlock Grid.Column="2" Text="ft"
                               FontSize="12" Foreground="#5A8C6A"
                               VerticalAlignment="Center" Margin="10,0,0,0"/>
				</Grid>

				<!-- Direction Options -->
				<StackPanel Grid.Row="2">
					<TextBlock Text="Offset Direction:"
                               FontSize="12" Foreground="#2D5D3A"
                               Margin="0,0,0,8"/>

					<RadioButton Name="OutwardRadioButton"
                                 Content="Outward - Create new floor outside existing boundary"
                                 Style="{StaticResource ModernRadioButton}"
                                 IsChecked="True"
                                 Checked="DirectionRadioButton_Checked"/>

					<RadioButton Name="InwardRadioButton"
                                 Content="Inward - Modify existing floor and create inner floor"
                                 Style="{StaticResource ModernRadioButton}"
                                 Checked="DirectionRadioButton_Checked"/>
				</StackPanel>
			</Grid>
		</Border>

		<!-- Floor Type Selection -->
		<Border Grid.Row="3" Style="{StaticResource Card}" Margin="20,10,20,10">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<!-- Section Title -->
				<TextBlock Grid.Row="0" Text="New Floor Type"
                           FontSize="16" FontWeight="SemiBold" Foreground="#2D5D3A"
                           Margin="0,0,0,15"/>

				<!-- Floor Type Dropdown -->
				<ComboBox Grid.Row="1" Name="FloorTypeComboBox"
                          Style="{StaticResource ModernComboBox}"
                          SelectionChanged="FloorTypeComboBox_SelectionChanged"/>

				<!-- Floor Type Info -->
				<TextBlock Grid.Row="2" Name="FloorTypeInfoTextBlock"
                           Text="Select a floor type for the new band"
                           Style="{StaticResource InfoText}" Margin="0,10,0,0"/>
			</Grid>
		</Border>

		<!-- Preview/Info Area -->
		<Border Grid.Row="4" Style="{StaticResource Card}" Margin="20,10,20,10">
			<ScrollViewer VerticalScrollBarVisibility="Auto">
				<StackPanel>
					<TextBlock Text="Configuration Summary"
                               FontSize="16" FontWeight="SemiBold" Foreground="#2D5D3A"
                               Margin="0,0,0,15"/>

					<TextBlock Name="SummaryTextBlock"
                               TextWrapping="Wrap" FontSize="12"
                               Foreground="#4A7C5A" LineHeight="18"
                               Text="Configure the settings above to see a summary"/>
				</StackPanel>
			</ScrollViewer>
		</Border>

		<!-- Footer with Actions -->
		<Border Grid.Row="5" Style="{StaticResource Card}" Margin="20,10,20,20">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>

				<!-- Status -->
				<TextBlock Grid.Column="0" Name="StatusTextBlock"
                           FontWeight="SemiBold" FontSize="14" Foreground="#2D5D3A"
                           Text="Configure settings to create floor band"
                           VerticalAlignment="Center"/>

				<!-- Action Buttons -->
				<StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
					<Button Name="CancelButton" Content="Cancel"
                            Style="{StaticResource ModernButton}"
                            Width="90" Height="38" Margin="0,0,12,0"
                            Click="CancelButton_Click"/>

					<Button Name="CreateBandButton" Content="🏗️ Create Floor Band"
                            Style="{StaticResource PrimaryButton}"
                            Width="160" Height="38"
                            Click="CreateBandButton_Click" IsEnabled="False"/>
				</StackPanel>
			</Grid>
		</Border>
	</Grid>
</Window>