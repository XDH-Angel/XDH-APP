<Window x:Class="LandscapeRevitAddIn.UI.Windows.Panel04.GraphMoundWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="XD Landscape - Graph Mound" Height="700" Width="800"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="White"
        WindowStyle="SingleBorderWindow"
        MinHeight="600" MinWidth="700">

	<Window.Resources>
		<!-- Logo-Inspired Gradient Background -->
		<LinearGradientBrush x:Key="LogoGradientBackground" StartPoint="0,0" EndPoint="1,1">
			<GradientStop Color="#B8D4C0" Offset="0"/>
			<GradientStop Color="#A8C9B3" Offset="0.3"/>
			<GradientStop Color="#8FBB9A" Offset="0.6"/>
			<GradientStop Color="#7AAE85" Offset="0.8"/>
			<GradientStop Color="#6BA177" Offset="1"/>
		</LinearGradientBrush>

		<!-- Card Background -->
		<SolidColorBrush x:Key="CardBackground" Color="#FEFEFE" Opacity="0.95"/>

		<!-- Modern Button Style -->
		<Style x:Key="ModernButton" TargetType="Button">
			<Setter Property="Background" Value="#4A7C5A"/>
			<Setter Property="Foreground" Value="White"/>
			<Setter Property="BorderBrush" Value="#3D6349"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Padding" Value="12,8"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
			<Setter Property="FontSize" Value="11"/>
			<Setter Property="Cursor" Value="Hand"/>
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
							<ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
						</Border>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="#5A8C6A"/>
				</Trigger>
				<Trigger Property="IsPressed" Value="True">
					<Setter Property="Background" Value="#3A5C45"/>
				</Trigger>
				<Trigger Property="IsEnabled" Value="False">
					<Setter Property="Background" Value="#CCCCCC"/>
					<Setter Property="Foreground" Value="#666666"/>
					<Setter Property="Cursor" Value="Arrow"/>
				</Trigger>
			</Style.Triggers>
		</Style>

		<!-- Primary Action Button -->
		<Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
			<Setter Property="Background" Value="#2D5D3A"/>
			<Setter Property="FontWeight" Value="Bold"/>
			<Setter Property="FontSize" Value="12"/>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="Background" Value="#4A7C5A"/>
				</Trigger>
				<Trigger Property="IsEnabled" Value="False">
					<Setter Property="Background" Value="#CCCCCC"/>
					<Setter Property="Foreground" Value="#666666"/>
				</Trigger>
			</Style.Triggers>
		</Style>

		<!-- Modern TextBox Style -->
		<Style x:Key="ModernTextBox" TargetType="TextBox">
			<Setter Property="Background" Value="White"/>
			<Setter Property="BorderBrush" Value="#A8C9B3"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Padding" Value="12,10"/>
			<Setter Property="FontSize" Value="12"/>
			<Setter Property="Height" Value="40"/>
			<Style.Triggers>
				<Trigger Property="IsMouseOver" Value="True">
					<Setter Property="BorderBrush" Value="#4A7C5A"/>
				</Trigger>
				<Trigger Property="IsFocused" Value="True">
					<Setter Property="BorderBrush" Value="#2D5D3A"/>
				</Trigger>
			</Style.Triggers>
		</Style>

		<!-- Modern ComboBox Style -->
		<Style x:Key="ModernComboBox" TargetType="ComboBox">
			<Setter Property="Background" Value="White"/>
			<Setter Property="BorderBrush" Value="#A8C9B3"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="Padding" Value="12,10"/>
			<Setter Property="FontSize" Value="12"/>
			<Setter Property="Height" Value="40"/>
		</Style>

		<!-- Card Style -->
		<Style x:Key="Card" TargetType="Border">
			<Setter Property="Background" Value="{StaticResource CardBackground}"/>
			<Setter Property="BorderBrush" Value="#C8DDD0"/>
			<Setter Property="BorderThickness" Value="1"/>
			<Setter Property="CornerRadius" Value="8"/>
			<Setter Property="Padding" Value="20"/>
			<Setter Property="Effect">
				<Setter.Value>
					<DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.15" Color="#4A7C5A"/>
				</Setter.Value>
			</Setter>
		</Style>

		<!-- Status Text Styles -->
		<Style x:Key="SuccessText" TargetType="TextBlock">
			<Setter Property="Foreground" Value="#2D5D3A"/>
			<Setter Property="FontWeight" Value="SemiBold"/>
		</Style>

		<Style x:Key="InfoText" TargetType="TextBlock">
			<Setter Property="Foreground" Value="#5A8C6A"/>
			<Setter Property="FontStyle" Value="Italic"/>
		</Style>
	</Window.Resources>

	<Grid Background="{StaticResource LogoGradientBackground}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<!-- Header -->
		<Border Grid.Row="0" Style="{StaticResource Card}" Margin="20,20,20,10">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto"/>
					<ColumnDefinition Width="*"/>
				</Grid.ColumnDefinitions>

				<!-- Logo -->
				<Border Grid.Column="0" Width="72" Height="72"
                        VerticalAlignment="Center" Margin="0,0,20,0">
					<Border.Background>
						<ImageBrush x:Name="LogoBrush" Stretch="Uniform"/>
					</Border.Background>
				</Border>

				<!-- Title and Info -->
				<StackPanel Grid.Column="1" VerticalAlignment="Center">
					<TextBlock Text="XD Landscape"
                               FontSize="24" FontWeight="Bold" Foreground="#2D5D3A"
                               Margin="0,0,0,5"/>
					<TextBlock Text="Create Graph Mound"
                               FontSize="14" Foreground="#4A7C5A"/>
					<TextBlock Text="Create mounds based on adjustable section graph and selected floor"
                               FontSize="11" Foreground="#5A8C6A" Margin="0,2,0,0"/>
				</StackPanel>
			</Grid>
		</Border>

		<!-- Floor Selection and Settings -->
		<Border Grid.Row="1" Style="{StaticResource Card}" Margin="20,10,20,10">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>

				<!-- Section Title -->
				<TextBlock Grid.Row="0" Text="Configuration"
                           FontSize="16" FontWeight="SemiBold" Foreground="#2D5D3A"
                           Margin="0,0,0,15"/>

				<!-- Floor Selection -->
				<Grid Grid.Row="1" Margin="0,0,0,15">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="2*"/>
						<ColumnDefinition Width="15"/>
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>

					<Button Grid.Column="0" Name="SelectFloorButton"
                            Content="📐 Select Floor to Update"
                            Style="{StaticResource ModernButton}"
                            Height="42" Click="SelectFloorButton_Click"/>

					<TextBlock Grid.Column="2" Name="SelectedFloorTextBlock"
                               Text="No floor selected"
                               Style="{StaticResource InfoText}"
                               VerticalAlignment="Center"/>
				</Grid>

				<!-- Settings Row 1 -->
				<Grid Grid.Row="2" Margin="0,0,0,15">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="*"/>
						<ColumnDefinition Width="15"/>
						<ColumnDefinition Width="*"/>
					</Grid.ColumnDefinitions>

					<StackPanel Grid.Column="0">
						<TextBlock Text="Direction:" FontSize="12" Foreground="#2D5D3A" Margin="0,0,0,5"/>
						<ComboBox Name="DirectionComboBox" Style="{StaticResource ModernComboBox}"
                                  SelectionChanged="DirectionComboBox_SelectionChanged">
							<ComboBoxItem Content="Horizontal (X-axis)" Tag="X" IsSelected="True"/>
							<ComboBoxItem Content="Vertical (Y-axis)" Tag="Y"/>
							<ComboBoxItem Content="Radial (from center)" Tag="Radial"/>
						</ComboBox>
					</StackPanel>

					<StackPanel Grid.Column="2">
						<TextBlock Text="Max Height (ft):" FontSize="12" Foreground="#2D5D3A" Margin="0,0,0,5"/>
						<TextBox Name="MaxHeightTextBox" Style="{StaticResource ModernTextBox}"
                                 Text="10.0" TextChanged="MaxHeightTextBox_TextChanged"/>
					</StackPanel>
				</Grid>

				<!-- Graph Controls -->
				<Grid Grid.Row="3">
					<Grid.ColumnDefinitions>
						<ColumnDefinition Width="Auto"/>
						<ColumnDefinition Width="15"/>
						<ColumnDefinition Width="Auto"/>
						<ColumnDefinition Width="15"/>
						<ColumnDefinition Width="Auto"/>
					</Grid.ColumnDefinitions>

					<Button Grid.Column="0" Name="ResetGraphButton"
                            Content="🔄 Reset Graph"
                            Style="{StaticResource ModernButton}"
                            Width="120" Height="35"
                            Click="ResetGraphButton_Click"/>

					<Button Grid.Column="2" Name="PresetHillButton"
                            Content="⛰️ Hill Preset"
                            Style="{StaticResource ModernButton}"
                            Width="120" Height="35"
                            Click="PresetHillButton_Click"/>

					<Button Grid.Column="4" Name="PresetValleyButton"
                            Content="🏞️ Valley Preset"
                            Style="{StaticResource ModernButton}"
                            Width="120" Height="35"
                            Click="PresetValleyButton_Click"/>
				</Grid>
			</Grid>
		</Border>

		<!-- Graph Editor -->
		<Border Grid.Row="2" Style="{StaticResource Card}" Margin="20,10,20,10">
			<StackPanel>
				<TextBlock Text="Section Profile Graph"
                           FontSize="16" FontWeight="SemiBold" Foreground="#2D5D3A"
                           Margin="0,0,0,10"/>
				<TextBlock Text="Click and drag to adjust the elevation profile. Left side = start, Right side = end"
                           FontSize="11" Foreground="#5A8C6A" Margin="0,0,0,15"/>
			</StackPanel>
		</Border>

		<!-- Interactive Graph Canvas -->
		<Border Grid.Row="3" Style="{StaticResource Card}" Margin="20,10,20,10">
			<Grid>
				<Border BorderBrush="#C8DDD0" BorderThickness="1" CornerRadius="4">
					<Canvas Name="GraphCanvas" Background="White" Height="200"
                            MouseLeftButtonDown="GraphCanvas_MouseLeftButtonDown"
                            MouseMove="GraphCanvas_MouseMove"
                            MouseLeftButtonUp="GraphCanvas_MouseLeftButtonUp"/>
				</Border>
			</Grid>
		</Border>

		<!-- Footer with Actions -->
		<Border Grid.Row="4" Style="{StaticResource Card}" Margin="20,10,20,20">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="*"/>
					<ColumnDefinition Width="Auto"/>
				</Grid.ColumnDefinitions>

				<!-- Status -->
				<StackPanel Grid.Column="0" VerticalAlignment="Center">
					<TextBlock Name="StatusTextBlock"
                               FontWeight="SemiBold" FontSize="14" Foreground="#2D5D3A"
                               Text="Select floor and configure graph to create mound"/>
					<TextBlock Text="The graph defines the elevation profile applied to the floor area"
                               FontSize="11" Foreground="#4A7C5A" Margin="0,2,0,0"/>
				</StackPanel>

				<!-- Action Buttons -->
				<StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
					<Button Name="CancelButton" Content="Cancel"
                            Style="{StaticResource ModernButton}"
                            Width="90" Height="38" Margin="0,0,12,0"
                            Click="CancelButton_Click"/>

					<Button Name="CreateMoundButton" Content="📊 Create Graph Mound"
                            Style="{StaticResource PrimaryButton}"
                            Width="180" Height="38"
                            Click="CreateMoundButton_Click" IsEnabled="False"/>
				</StackPanel>
			</Grid>
		</Border>
	</Grid>
</Window>