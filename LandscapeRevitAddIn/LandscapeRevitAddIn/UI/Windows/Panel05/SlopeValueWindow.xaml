<!-- UI/Windows/Panel05/SlopeValueWindow.xaml -->
<Window x:Class="LandscapeRevitAddIn.UI.Windows.Panel05.SlopeValueWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="XD Landscape - Slope Value" Height="550" Width="650"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        Background="White"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>
        <!-- Logo-Inspired Gradient Background -->
        <LinearGradientBrush x:Key="LogoGradientBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#B8D4C0" Offset="0"/>
            <GradientStop Color="#A8C9B3" Offset="0.3"/>
            <GradientStop Color="#8FBB9A" Offset="0.6"/>
            <GradientStop Color="#7AAE85" Offset="0.8"/>
            <GradientStop Color="#6BA177" Offset="1"/>
        </LinearGradientBrush>

        <!-- Subtle Card Background -->
        <SolidColorBrush x:Key="CardBackground" Color="#FEFEFE" Opacity="0.95"/>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4A7C5A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3D6349"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5A8C6A"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3A5C45"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Primary Action Button -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#2D5D3A"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A7C5A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern ComboBox Style -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#A8C9B3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#A8C9B3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Height" Value="40"/>
        </Style>

        <!-- Card Style -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="#C8DDD0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.15" Color="#4A7C5A"/>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource LogoGradientBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="20,20,20,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Width="72" Height="72" VerticalAlignment="Center" Margin="0,0,20,0">
                    <Border.Background>
                        <ImageBrush x:Name="LogoBrush" Stretch="Uniform"/>
                    </Border.Background>
                </Border>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="XD Landscape" FontSize="24" FontWeight="Bold" Foreground="#2D5D3A" Margin="0,0,0,5"/>
                    <TextBlock Text="Apply Slope to Floor" FontSize="14" Foreground="#4A7C5A"/>
                    <TextBlock Text="Define slope direction and value (percentage or degree) for floor adjustment" FontSize="11" Foreground="#5A8C6A" Margin="0,2,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Floor Selection -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="20,10,20,10">
            <StackPanel>
                <TextBlock Text="Floor Selection" FontSize="16" FontWeight="SemiBold" Foreground="#2D5D3A" Margin="0,0,0,15"/>

                <TextBlock Text="Select Floor to Apply Slope:" FontSize="12" Foreground="#4A7C5A" Margin="0,0,0,5"/>
                <ComboBox Name="FloorComboBox" Style="{StaticResource ModernComboBox}" SelectionChanged="FloorComboBox_SelectionChanged"/>

                <TextBlock Name="FloorInfoTextBlock" Margin="0,12,0,0" Text="Choose floor to apply slope" Foreground="#5A8C6A" FontStyle="Italic"/>
            </StackPanel>
        </Border>

        <!-- Direction Selection -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="20,10,20,10">
            <StackPanel>
                <TextBlock Text="Slope Direction" FontSize="16" FontWeight="SemiBold" Foreground="#2D5D3A" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Direction Buttons Grid -->
                    <StackPanel Grid.Column="0" Margin="0,0,10,0">
                        <TextBlock Text="Select Direction:" FontSize="12" Foreground="#4A7C5A" Margin="0,0,0,10"/>

                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- Direction buttons arranged in compass layout -->
                            <Button Grid.Row="0" Grid.Column="1" Name="NorthButton" Content="↑ N" Height="35" Margin="2" Style="{StaticResource ModernButton}" Click="DirectionButton_Click" Tag="North"/>
                            <Button Grid.Row="1" Grid.Column="0" Name="WestButton" Content="← W" Height="35" Margin="2" Style="{StaticResource ModernButton}" Click="DirectionButton_Click" Tag="West"/>
                            <Button Grid.Row="1" Grid.Column="2" Name="EastButton" Content="→ E" Height="35" Margin="2" Style="{StaticResource ModernButton}" Click="DirectionButton_Click" Tag="East"/>
                            <Button Grid.Row="2" Grid.Column="1" Name="SouthButton" Content="↓ S" Height="35" Margin="2" Style="{StaticResource ModernButton}" Click="DirectionButton_Click" Tag="South"/>
                        </Grid>
                    </StackPanel>

                    <!-- Custom Direction -->
                    <StackPanel Grid.Column="1" Margin="10,0,0,0">
                        <TextBlock Text="Custom Direction (degrees):" FontSize="12" Foreground="#4A7C5A" Margin="0,0,0,5"/>
                        <TextBox Name="DirectionTextBox" Style="{StaticResource ModernTextBox}" Text="0" TextChanged="DirectionTextBox_TextChanged"/>
                        <TextBlock Text="0° = North, 90° = East" FontSize="10" Foreground="#7AAE85" Margin="0,5,0,0" FontStyle="Italic"/>
                    </StackPanel>
                </Grid>

                <TextBlock Name="DirectionInfoTextBlock" Margin="0,12,0,0" Text="Select slope direction using buttons or enter custom angle" Foreground="#5A8C6A" FontStyle="Italic"/>
            </StackPanel>
        </Border>

        <!-- Slope Value -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Margin="20,10,20,10">
            <StackPanel>
                <TextBlock Text="Slope Value" FontSize="16" FontWeight="SemiBold" Foreground="#2D5D3A" Margin="0,0,0,15"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Margin="0,0,15,0">
                        <TextBlock Text="Slope Value:" FontSize="12" Foreground="#4A7C5A" Margin="0,0,0,5"/>
                        <TextBox Name="SlopeValueTextBox" Style="{StaticResource ModernTextBox}" Text="2.0" TextChanged="SlopeValueTextBox_TextChanged"/>
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="Unit:" FontSize="12" Foreground="#4A7C5A" Margin="0,0,0,5"/>
                        <ComboBox Name="UnitComboBox" Style="{StaticResource ModernComboBox}" SelectedIndex="0" SelectionChanged="UnitComboBox_SelectionChanged">
                            <ComboBoxItem Content="Percentage (%)" Tag="Percentage"/>
                            <ComboBoxItem Content="Degrees (°)" Tag="Degrees"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <TextBlock Name="SlopeInfoTextBlock" Margin="0,12,0,0" Text="Enter slope value and select unit (percentage or degrees)" Foreground="#5A8C6A" FontStyle="Italic"/>
            </StackPanel>
        </Border>

        <!-- Footer -->
        <Border Grid.Row="4" Style="{StaticResource Card}" Margin="20,10,20,20">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Name="SummaryTextBlock" FontWeight="SemiBold" FontSize="14" Foreground="#2D5D3A" Text="Configure floor, direction and slope value to continue"/>
                    <TextBlock Text="Floor will be modified to have the specified slope in the chosen direction" FontSize="11" Foreground="#4A7C5A" Margin="0,2,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Name="CancelButton" Content="Cancel" Style="{StaticResource ModernButton}" Width="90" Height="38" Margin="0,0,12,0" Click="CancelButton_Click"/>
                    <Button Name="ApplySlopeButton" Content="📐 Apply Slope" Style="{StaticResource PrimaryButton}" Width="140" Height="38" Click="ApplySlopeButton_Click" IsEnabled="False"/>
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>