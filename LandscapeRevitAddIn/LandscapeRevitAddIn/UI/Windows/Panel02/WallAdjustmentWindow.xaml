<Window x:Class="LandscapeRevitAddIn.UI.Windows.Panel02.WallAdjustmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="XD Landscape - Wall Level and Height Adjustment" Height="650" Width="600"
        ResizeMode="CanResize" WindowStartupLocation="CenterOwner"
        Background="White" WindowStyle="SingleBorderWindow"
        MinHeight="550" MinWidth="500"
        Loaded="Window_Loaded">

    <Window.Resources>
        <!-- Logo-Inspired Gradient Background -->
        <LinearGradientBrush x:Key="LogoGradientBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#B8D4C0" Offset="0"/>
            <GradientStop Color="#A8C9B3" Offset="0.3"/>
            <GradientStop Color="#8FBB9A" Offset="0.6"/>
            <GradientStop Color="#7AAE85" Offset="0.8"/>
            <GradientStop Color="#6BA177" Offset="1"/>
        </LinearGradientBrush>

        <!-- Subtle Card Background with slight transparency to show gradient -->
        <SolidColorBrush x:Key="CardBackground" Color="#FEFEFE" Opacity="0.95"/>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#4A7C5A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#3D6349"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5A8C6A"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3A5C45"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#666666"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Primary Action Button -->
        <Style x:Key="PrimaryButton" TargetType="Button" BasedOn="{StaticResource ModernButton}">
            <Setter Property="Background" Value="#2D5D3A"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="12"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#4A7C5A"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#CCCCCC"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Modern ComboBox Style with Green Accents -->
        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#A8C9B3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8,6"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border Name="Border" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3"/>
                            <ToggleButton Name="ToggleButton" 
                                        Grid.Column="2" 
                                        Focusable="false"
                                        IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}"
                                        ClickMode="Press"
                                        Background="Transparent"
                                        BorderThickness="0">
                                <Path Name="Arrow"
                                      HorizontalAlignment="Center"
                                      VerticalAlignment="Center"
                                      Data="M 0 0 L 3 3 L 6 0 Z"
                                      Fill="#4A7C5A" />
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite"
                                            IsHitTestVisible="False" 
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="8,0,20,0"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left" />
                            <Popup Name="Popup"
                                   Placement="Bottom"
                                   IsOpen="{TemplateBinding IsDropDownOpen}"
                                   AllowsTransparency="True" 
                                   Focusable="False"
                                   PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                      SnapsToDevicePixels="True"                
                                      MinWidth="{TemplateBinding ActualWidth}"
                                      MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border Name="DropDownBorder"
                                            Background="White"
                                            BorderThickness="1"
                                            BorderBrush="#A8C9B3"
                                            CornerRadius="3"/>
                                    <ScrollViewer Margin="2,4,2,4" SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True" KeyboardNavigation.DirectionalNavigation="Contained" />
                                    </ScrollViewer>
                                </Grid>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4A7C5A"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2D5D3A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Padding" Value="8,4"/>
                        <Setter Property="FontSize" Value="11"/>
                        <Setter Property="MinHeight" Value="24"/>
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E8F4EB"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Background" Value="#D4E8D8"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern TextBox Style -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#A8C9B3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6,4"/>
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Name="Border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#4A7C5A"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="#2D5D3A"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#F0F0F0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern CheckBox Style -->
        <Style x:Key="ModernCheckBox" TargetType="CheckBox">
            <Setter Property="Foreground" Value="#2D5D3A"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="6,0,0,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="CheckBox">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" Name="checkBorder" 
                                    Width="14" Height="14" 
                                    BorderBrush="#A8C9B3" BorderThickness="1.5"
                                    Background="White" CornerRadius="2">
                                <Path Name="checkMark" 
                                      Data="M 2 5 L 5 8 L 11 2" 
                                      Stroke="#2D5D3A" StrokeThickness="1.5"
                                      Visibility="Collapsed"/>
                            </Border>

                            <ContentPresenter Grid.Column="1" 
                                            Margin="{TemplateBinding Padding}"
                                            VerticalAlignment="Center"/>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="checkMark" Property="Visibility" Value="Visible"/>
                                <Setter TargetName="checkBorder" Property="Background" Value="#E8F4EB"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="checkBorder" Property="BorderBrush" Value="#4A7C5A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern GroupBox Style -->
        <Style x:Key="ModernGroupBox" TargetType="GroupBox">
            <Setter Property="BorderBrush" Value="#C8DDD0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="GroupBox">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Background="#E8F4EB" 
                                    BorderBrush="#C8DDD0" BorderThickness="1"
                                    CornerRadius="4,4,0,0" Padding="8,4">
                                <ContentPresenter ContentSource="Header"
                                                TextBlock.Foreground="#2D5D3A"
                                                TextBlock.FontWeight="SemiBold"
                                                TextBlock.FontSize="12"/>
                            </Border>

                            <Border Grid.Row="1" Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="1,0,1,1"
                                    CornerRadius="0,0,4,4"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter/>
                            </Border>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Updated Card Style with subtle transparency -->
        <Style x:Key="Card" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="#C8DDD0"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect ShadowDepth="2" BlurRadius="8" Opacity="0.15" Color="#4A7C5A"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Status Text Styles with Green Theme -->
        <Style x:Key="SuccessText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#2D5D3A"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="ErrorText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#C85450"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="InfoText" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#5A8C6A"/>
            <Setter Property="FontStyle" Value="Italic"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource LogoGradientBackground}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Style="{StaticResource Card}" Margin="20,20,20,10">
            <StackPanel>
                <TextBlock Text="XD Landscape"
                           FontSize="20" FontWeight="Bold" Foreground="#2D5D3A"
                           Margin="0,0,0,5"/>
                <TextBlock Text="Wall Level and Height Adjustment"
                           FontSize="14" Foreground="#4A7C5A" Margin="0,0,0,5"/>
                <TextBlock x:Name="WallCountTextBlock" Text="Selected Walls: 0"
                           Style="{StaticResource InfoText}"/>
            </StackPanel>
        </Border>

        <!-- Info Panel -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="20,10,20,10">
            <Border Background="#FFF9E6" BorderBrush="#D4C69A" BorderThickness="1" 
                    CornerRadius="4" Padding="12">
                <StackPanel>
                    <TextBlock FontSize="11" TextWrapping="Wrap" Foreground="#8B7355">
                        <Run Text="📋 Note: " FontWeight="Bold"/>
                        <Run FontWeight="Bold" Text="All measurements are in millimeters."/>
                        <Run Text=" Adjustments will be applied to all selected walls."/>
                    </TextBlock>
                    <TextBlock Text="Base and top offsets are relative adjustments to current values."
                               FontSize="10" Foreground="#8B7355" Margin="0,3,0,0"/>
                </StackPanel>
            </Border>
        </Border>

        <!-- Main Content -->
        <Border Grid.Row="2" Style="{StaticResource Card}" Margin="20,10,20,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>

                    <!-- Base Level Section -->
                    <GroupBox Header="🏗️ Base Level Adjustment" Style="{StaticResource ModernGroupBox}" Margin="0,0,0,15">
                        <StackPanel>
                            <CheckBox x:Name="AdjustBaseLevelCheckBox" Content="Adjust Base Level"
                                      Style="{StaticResource ModernCheckBox}"
                                      Checked="AdjustBaseLevelCheckBox_Checked"
                                      Unchecked="AdjustBaseLevelCheckBox_Unchecked"
                                      Margin="0,5,0,10"/>

                            <StackPanel x:Name="BaseLevelPanel" IsEnabled="False" Margin="0,0,0,5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Target Level:"
                                               VerticalAlignment="Center" Margin="0,0,10,8"
                                               FontSize="11" Foreground="#2D5D3A"/>
                                    <ComboBox x:Name="BaseLevelComboBox" Grid.Row="0" Grid.Column="1"
                                              Style="{StaticResource ModernComboBox}"
                                              Margin="0,0,0,8" Height="24"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Offset (mm):"
                                               VerticalAlignment="Center" Margin="0,0,10,0"
                                               FontSize="11" Foreground="#2D5D3A"/>
                                    <TextBox x:Name="BaseOffsetTextBox" Grid.Row="1" Grid.Column="1"
                                             Style="{StaticResource ModernTextBox}"
                                             Text="0" Width="100" HorizontalAlignment="Left" Height="24"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <!-- Top Level Section -->
                    <GroupBox Header="🏢 Top Level Adjustment" Style="{StaticResource ModernGroupBox}" Margin="0,0,0,15">
                        <StackPanel>
                            <CheckBox x:Name="AdjustTopLevelCheckBox" Content="Adjust Top Level"
                                      Style="{StaticResource ModernCheckBox}"
                                      Checked="AdjustTopLevelCheckBox_Checked"
                                      Unchecked="AdjustTopLevelCheckBox_Unchecked"
                                      Margin="0,5,0,10"/>

                            <StackPanel x:Name="TopLevelPanel" IsEnabled="False" Margin="0,0,0,5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Target Level:"
                                               VerticalAlignment="Center" Margin="0,0,10,8"
                                               FontSize="11" Foreground="#2D5D3A"/>
                                    <ComboBox x:Name="TopLevelComboBox" Grid.Row="0" Grid.Column="1"
                                              Style="{StaticResource ModernComboBox}"
                                              Margin="0,0,0,8" Height="24"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Offset (mm):"
                                               VerticalAlignment="Center" Margin="0,0,10,0"
                                               FontSize="11" Foreground="#2D5D3A"/>
                                    <TextBox x:Name="TopOffsetTextBox" Grid.Row="1" Grid.Column="1"
                                             Style="{StaticResource ModernTextBox}"
                                             Text="0" Width="100" HorizontalAlignment="Left" Height="24"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                    <!-- Height Adjustment Section -->
                    <GroupBox Header="📏 Height Adjustment" Style="{StaticResource ModernGroupBox}" Margin="0,0,0,10">
                        <StackPanel>
                            <CheckBox x:Name="AdjustHeightCheckBox" Content="Adjust Unconnected Height"
                                      Style="{StaticResource ModernCheckBox}"
                                      Checked="AdjustHeightCheckBox_Checked"
                                      Unchecked="AdjustHeightCheckBox_Unchecked"
                                      Margin="0,5,0,10"/>

                            <StackPanel x:Name="HeightPanel" IsEnabled="False" Margin="0,0,0,5">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="110"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Current Height:"
                                               VerticalAlignment="Center" Margin="0,0,10,8"
                                               FontSize="11" Foreground="#2D5D3A"/>
                                    <TextBox x:Name="CurrentHeightTextBox" Grid.Row="0" Grid.Column="1"
                                             Style="{StaticResource ModernTextBox}"
                                             IsReadOnly="True" Width="100" HorizontalAlignment="Left"
                                             Margin="0,0,0,8" Height="24"/>

                                    <TextBlock Grid.Row="1" Grid.Column="0" Text="Adjustment (mm):"
                                               VerticalAlignment="Center" Margin="0,0,10,0"
                                               FontSize="11" Foreground="#2D5D3A"/>
                                    <TextBox x:Name="HeightAdjustmentTextBox" Grid.Row="1" Grid.Column="1"
                                             Style="{StaticResource ModernTextBox}"
                                             Text="0" Width="100" HorizontalAlignment="Left" Height="24"/>
                                </Grid>
                            </StackPanel>
                        </StackPanel>
                    </GroupBox>

                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Quick Actions -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Margin="20,10,20,10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="✓ Select All" Click="SelectAllButton_Click"
                        Style="{StaticResource ModernButton}"
                        Width="90" Height="28" Margin="0,0,8,0"/>
                <Button Content="✗ Clear All" Click="ClearAllButton_Click"
                        Style="{StaticResource ModernButton}"
                        Width="90" Height="28" Margin="0,0,8,0"/>
                <Button Content="🔄 Reset Defaults" Click="ResetToDefaultsButton_Click"
                        Style="{StaticResource ModernButton}"
                        Width="110" Height="28"/>
            </StackPanel>
        </Border>

        <!-- Dialog Buttons -->
        <Border Grid.Row="4" Style="{StaticResource Card}" Margin="20,10,20,20">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="🏗️ OK" Click="OkButton_Click" IsDefault="True"
                        Style="{StaticResource PrimaryButton}"
                        Width="85" Height="32" Margin="0,0,10,0"/>
                <Button Content="Cancel" Click="CancelButton_Click" IsCancel="True"
                        Style="{StaticResource ModernButton}"
                        Width="85" Height="32"/>
            </StackPanel>
        </Border>
    </Grid>
</Window>